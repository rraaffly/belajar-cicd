import { expect, test } from "bun:test";

test("Penjumlahan", () => {
  expect(2 + 9).toBe(11);
});

test("Pengurangan", () => {
  expect(7 - 2).toBe(5);
});

test("Pembagian", () => {
  expect(3 / 3).toBe(1);
});

test("Perkalian", () => {
  expect(2 * 2).toBe(4);
});
